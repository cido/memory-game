var React = require("../lib/react"),
    State = require("../State");

module.exports = React.createClass({
    render: function() {
        return <div className="board game-board">
            Where are all our cards?
        </div>
    }
});
